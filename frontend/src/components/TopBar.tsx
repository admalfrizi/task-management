const TopBar = () => {
    return (
        <>
            <nav className={`navbar px-5 md:px-24 w-screen fixed transition-colors ease duration-500 inset-0 flex flex-row justify-between items-center h-16 z-50 `}>
                <div className="">
					
				</div>
            </nav>
        </>
    )
}

export default TopBar